/**
 * Tipos de `ViewTransition` y `addTransitionType`.
 *
 * El runtime ya los trae: Next 16 usa internamente el canal canary de React en
 * el App Router (ver `node_modules/next/dist/docs/01-app/02-guides/view-transitions.md`).
 * Lo que falta son los tipos, que `@types/react` publica aparte en `canary.d.ts`.
 */
/// <reference types="react/canary" />
